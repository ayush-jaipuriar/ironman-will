APP_PORT=8081

# GCP / GCS
GCP_PROJECT=your-gcp-project
GCP_REGION=us-central1
GCS_BUCKET=iron-will-proofs

# Database (Cloud SQL Postgres with pgvector)
DB_URL=postgresql+psycopg://postgres:postgres@localhost:5432/iron_will
DB_USERNAME=postgres
DB_PASSWORD=postgres

# Agent internal auth
AGENT_INTERNAL_SECRET=change-me

# Gemini / Vertex AI
GEMINI_PROJECT=your-gcp-project
GEMINI_LOCATION=us-central1
GEMINI_MODEL=gemini-2.5-flash-lite-preview

# Rate limiting
RATE_LIMIT_PER_MIN=60

